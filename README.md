# Portfolio Site

A clean static personal portfolio site built with HTML, CSS, and JavaScript.

## Run

Open `index.html` in a browser.

## Publish

This project works well with GitHub Pages.

```powershell
git init
git add .
git commit -m "Initial portfolio site"
gh repo create portfolio-site --public --source=. --remote=origin --push
```

